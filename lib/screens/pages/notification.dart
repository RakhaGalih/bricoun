import 'package:flutter/material.dart';

class Notifcation extends StatelessWidget {
  const Notifcation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('notifikasi'),));
  }
}
